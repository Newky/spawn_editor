from setuptools import setup

setup(name='spawn',
      version='0.1',
      description='Python module to spawn $EDITOR for user input in a temporary file. (think git commit without the -m) ',
      url='http://github.com/Newky/spawn_editor',
      author='Richard Delaney',
      author_email='richdel1991@gmail.com',
      license='MIT',
      packages=['spawn'],
      zip_safe=False)
